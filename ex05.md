```mermaid
sequenceDiagram;

  participant user
  participant browser
  participant server
  
  browser->>server: HTTP POST request for JSON raw data
  server->>browser: status code 201 created, no redirection
  
  note add notes HTML elements by using DOM API
